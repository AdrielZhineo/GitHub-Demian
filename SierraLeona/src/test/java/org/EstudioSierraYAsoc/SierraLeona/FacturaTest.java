package org.EstudioSierraYAsoc.SierraLeona;

import java.util.ArrayList;
import java.util.List;

import org.junit.Assert;
import org.junit.Before;
import org.junit.Test;

import modelo.Cliente;
import modelo.Factura;
import modelo.Item;
import modelo.Usuario;

public class FacturaTest {

	/*Cliente cliente;
	Factura factura;
	Usuario user;
	Item item1, item2, item3;
	List<Item> items;
	
	@Before
	public void init() {
		item1 = new Item(); item1.cantidad = 10; item1.precio = 2;
		item2 = new Item(); item2.cantidad = 5; item2.precio = 2;
		item3 = new Item(); item3.cantidad = 5; item3.precio = 2;
		items = new ArrayList<>();
		items.add(item1);
		items.add(item2);
		items.add(item3);
		factura = new Factura(3, user, cliente, items, null);
	}
	
	@Test
	public void calcularTotal() {
		Assert.assertEquals(40, factura.total(), 2);
	}*/ 
	
	
}

